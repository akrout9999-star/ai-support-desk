{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "description": "SUPPORT/OS API for authenticated ticketing and AI-assisted support operations.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check routes/auth.js && node --check routes/tickets.js && node --check middleware/auth.js && node --check database/db.js && node --check services/aiService.js"
  },
  "engines": {
    "node": ">=20"
  },
  "type": "commonjs",
  "dependencies": {
    "@google/genai": "^2.22.0",
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^13.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  }
}
